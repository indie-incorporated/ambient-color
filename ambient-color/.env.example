# Copy this to .env and fill in your key.
# For a pure HTML file, the key is stored in localStorage at runtime.
# If you ever add a build step or backend proxy, wire this in there.

OPENWEATHERMAP_API_KEY=your_api_key_here
